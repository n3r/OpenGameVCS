# Completed PRDs

No PRD is complete yet.

Move a PRD here only after its completion evidence satisfies the lifecycle and Definition of Done in [`../README.md`](../README.md). Preserve its ID and filename. Every general completion-evidence category and every acceptance-criterion ID must contain a durable Markdown link; `TBD`, `N/A`, placeholders, and unlinked assertions are rejected.
