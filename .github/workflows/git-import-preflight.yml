name: R1 Git import preflight portability

on:
  pull_request:
    paths:
      - '.github/workflows/git-import-preflight.yml'
      - 'core/import-git-lfs/rust/**'
      - 'core/object-model/rust/**'
      - 'core/paths-filesystem/rust/**'
      - 'docs/reviews/OGVCS-020-git-import-preflight-boundary-review.md'
      - 'docs/evidence/OGVCS-020/**'
      - 'prd/todo/OGVCS-020-git-lfs-importer.md'
      - 'tools/git-import-preflight-policy.test.mjs'
      - 'package.json'
      - 'package-lock.json'
  push:
    branches: [main, r1-foundation-integration]
    paths:
      - '.github/workflows/git-import-preflight.yml'
      - 'core/import-git-lfs/rust/**'
      - 'core/object-model/rust/**'
      - 'core/paths-filesystem/rust/**'
      - 'docs/reviews/OGVCS-020-git-import-preflight-boundary-review.md'
      - 'docs/evidence/OGVCS-020/**'
      - 'prd/todo/OGVCS-020-git-lfs-importer.md'
      - 'tools/git-import-preflight-policy.test.mjs'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: git-import-preflight-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-portability:
    name: Private preflight portability (${{ matrix.label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - { runner: ubuntu-latest, label: Linux }
          - { runner: macos-latest, label: macOS }
          - { runner: windows-latest, label: Windows }
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - uses: dtolnay/rust-toolchain@7d11e79e1714f6b6da93cac39ad8435666f5c337 # 1.82.0
        with:
          components: clippy, rustfmt
      - run: node --test tools/git-import-preflight-policy.test.mjs
      - run: cargo fetch --manifest-path core/import-git-lfs/rust/Cargo.toml --locked
      - run: cargo fmt --manifest-path core/import-git-lfs/rust/Cargo.toml -- --check
      - run: cargo test --manifest-path core/import-git-lfs/rust/Cargo.toml --locked --offline
      - run: cargo test --manifest-path core/import-git-lfs/rust/Cargo.toml --locked --offline --release
      - run: cargo clippy --manifest-path core/import-git-lfs/rust/Cargo.toml --locked --offline --all-targets -- -D warnings
      - run: sh ./scripts/test-packed.sh
        shell: bash
        working-directory: core/import-git-lfs/rust
      - name: Validate the open roadmap boundary
        if: runner.os == 'Linux'
        run: node prd/validate-roadmap.mjs && node --test prd/validate-roadmap.test.mjs
