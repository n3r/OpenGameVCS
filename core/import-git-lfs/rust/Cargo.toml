[package]
name = "ogvcs-git-import-preflight"
version = "0.1.0-rc.1"
edition = "2021"
rust-version = "1.82"
license = "MIT"
description = "Private bounded Git import and Git LFS preflight candidate for OpenGameVCS"
readme = "README.md"
publish = false
include = [
    "src/**",
    "scripts/**",
    "tests/**",
    "Cargo.toml",
    "Cargo.lock",
    "LICENSE",
    "README.md",
]

[lib]
path = "src/lib.rs"

[dependencies]
ogvcs-object-model = { path = "../../object-model/rust", version = "0.1.0" }
ogvcs-path-contract = { path = "../../paths-filesystem/rust", version = "1.0.0" }

[dev-dependencies]
serde_json = "=1.0.151"
sha2 = "=0.10.9"
